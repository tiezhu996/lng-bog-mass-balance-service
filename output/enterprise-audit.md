# 企业级复审记录

- 项目：gb-529 lng-boiloff-gas-balance
- 复审日期：2026-08-22
- 范围：现有 `output/execution.md`、JWT/RBAC、质量平衡状态机、计算快照、审计与持久化边界。

## 结论

已修复两项高优先级缺陷。

1. 认证现在在 JWT 验签后按用户 ID 查询当前账号状态和角色，因此停用和角色变更立即生效，不再让旧 token 保留旧权限。
2. 质量平衡运行此前先以 `queued` 状态在一个事务中创建，再在第二个事务中写入计算结果。第二阶段失败或进程中断会遗留半成品运行。现在最终 `calculating` 运行、不可变输入/证据快照以及 queued/calculated 两条审计事件在单一事务中写入，失败会整体回滚。

质量平衡输入快照、版本条件更新和状态转换规则均已复查；未发现额外可证实的高优先级缺陷。

## 回归验证

- 新增认证实时撤权与角色刷新测试。
- `go test ./backend/...`：通过。
- `go vet ./backend/...`：通过。
- 本轮未启动或重启 Compose 服务。
