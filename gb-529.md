# gb-529 LNG 蒸发气物理质量平衡

请生成英文短名为 `lng-boiloff-gas-balance` 的 Go 全栈 Web 项目。系统供 LNG 储运技术人员录入离线罐容、计量快照和转移动作，计算指定期间的物理质量平衡、蒸发气估算和不确定度区间并提交复核。系统不连接 DCS、SIS、阀门或仪表，不下发控制动作，不用于贸易结算或财务对账，结果仅作工程分析支持。项目不含库存台账、订单、采购、工单或计费功能。

## 项目主要需求

### 1. 核心实体

- `StorageTank`：字段至少含 `id`、`tank_code`、`name`、`nominal_capacity_m3`、`min_level_m`、`max_level_m`、`reference_density_kgm3`、`tank_status`、`version`；表示计算边界而非库存，贯穿全栈。
- `MeasurementSnapshot`：字段至少含 `id`、`tank_id`、`measured_at`、`liquid_level_m`、`liquid_temp_c`、`vapor_pressure_kpa`、`density_kgm3`、`measurement_uncertainty_pct`、`quality_flag`、`source_note`；原值不可覆盖，贯穿全栈。
- `TransferOperation`：字段至少含 `id`、`tank_id`、`operation_type`、`start_at`、`end_at`、`measured_mass_kg`、`measurement_uncertainty_pct`、`counterparty_ref`、`operation_status`、`version`；只表示物理流入流出，不做商业订单，贯穿全栈。
- `BalanceRun`：字段至少含 `id`、`tank_id`、`period_start`、`period_end`、`balance_status`、`input_snapshot_json`、`opening_mass_kg`、`closing_mass_kg`、`net_transfer_kg`、`estimated_bog_kg`、`uncertainty_kg`、`deviation_pct`、`evidence_json`、`reviewed_by`；结果可重放且不可覆盖，贯穿全栈。

### 2. 核心页面

- `/tanks` 储罐参数页：消费 `StorageTank`、`MeasurementSnapshot`，维护计算边界并查看快照质量。
- `/measurements` 计量快照页：消费 `MeasurementSnapshot`、`StorageTank`，录入/校验液位温度密度。
- `/transfers` 物理转移页：消费 `TransferOperation`、`StorageTank`，维护期间流入流出和不确定度。
- `/balances` 平衡工作台：消费 `BalanceRun`、`MeasurementSnapshot`、`TransferOperation`，运行计算并解释差异与证据。
- `/audit` 审计复核页：消费 `BalanceRun`、`StorageTank`，比较输入版本、提交/批准和查询审计事件。

### 3. 差异算法与状态流

- 使用可测试的罐容换算函数将液位转换为体积，再结合温度修正密度计算液相质量；罐容曲线和修正系数配置化并写入运行快照。
- 质量平衡公式明确为：期初质量 + 期间流入 - 期间流出 - 期末质量 = 估算蒸发气及未解释项；所有单位统一并在边界处校验。
- 使用独立误差传播计算期初、期末和转移计量的不确定度合成区间，输出实际偏差与不确定区间关系；不得把未解释差异直接认定为泄漏或安全事件。
- `BalanceRun` 状态为 `queued -> calculating -> pending_review -> accepted | rejected | invalidated`；输入快照变化不会篡改旧结果，新运行另建记录，接受只允许 reviewer 且事务化。

### 4. 横切关注点

- JWT/RBAC 角色 `process_analyst`、`reviewer`、`admin`，贯穿角色数据、中间件、路由、前端守卫/按钮和类型。
- 审计覆盖罐参数、计量快照、转移、系数版本、平衡运行与复核，包含 request ID、操作者、前后摘要。
- 全局错误处理统一单位/范围错误、缺失边界快照、时间重叠、状态冲突、版本冲突和无权限。
- 请求追踪、结构化访问日志、panic recovery、本地限流独立实现；日志不得记录 JWT 或数据库凭据。

### 5. 共享枚举、组件、hooks 与中间件

- `BalanceStatus = queued | calculating | pending_review | accepted | rejected | invalidated`：后端 `backend/internal/constants/balance.go`，前端 `frontend/src/types/balance.ts`。
- `DeviationLevel = within_uncertainty | watch | investigate | invalid`：后端 `backend/internal/constants/deviation.go`，前端 `frontend/src/types/deviation.ts`。
- README 列明枚举在数据库、后端各层与前端 store/component/page 的全部位置。
- 共享组件：`QualityFlagBadge`（计量/平衡页）、`MassBalanceWaterfall`（罐/平衡页）、`EvidenceBreakdownPanel`（平衡/审计页）。
- hooks：`useAuth`、`useBalanceRun`，集中处理权限、提交和轮询。
- 中间件至少独立实现 `RequestID`、`AccessLog`、`Auth`、`RBAC`、`Recovery`、`RateLimit`。

### 6. 技术与规模红线

- React 18 + TypeScript + Vite + Ant Design + Zustand + ECharts，计算图表必须使用真实 API 数据。
- Go 1.22 + Gin + GORM + PostgreSQL；validator/v10、结构化日志、构造器注入、事务、`%w` 和优雅停机。
- 2500-4200 行功能 Go 代码、24-42 个功能 `.go` 文件且低于 5000 行，不计测试/vendor/生成代码/前端；禁止空壳或职责合并。
- API `/api/v1`，真实 `/healthz`、`/readyz`、统一响应/分页/错误码；单位换算、罐容曲线、质量平衡、误差传播和状态机需测试。
- 禁止 TODO、placeholder、随机数伪造计量、贸易结算逻辑、设备控制或 handler 直连数据库。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,balance,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

实体逐层分文件；单位换算、罐容曲线、质量方程、不确定度各自分文件。严禁合并职责到单一文件。

`runtime_smoke.json` 只能作为服务启动 manifest，禁止承载构建、API、Browser 或截图结果。必须使用以下内容，并实现 smoke GORM SQLite 与 Compose PostgreSQL 两种配置：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20529",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb529-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20529/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 名 `lng-boiloff-gas-balance`；前端 `18529`、后端 `19529`、PostgreSQL `57529`，容器内 `80/8080/5432`。
- Compose 顶层 `name`、无 `version:`，容器名带 `${COMPOSE_PROJECT_NAME:-lng-boiloff-gas-balance}`；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env`/示例含 Compose 名、端口、数据库、JWT、CORS、默认不确定度阈值和日志级别。Nginx `/api` 反代与 SPA fallback；中文目录一键启动。
- README 中文、Docker 启动优先，含复制 env、地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、公式/单位/假设、安全边界、开发命令、排错和 License。

### 9. 强制真实验收

1. 根目录执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/project_scale.py .` 和 `python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/runtime_smoke.py .`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端完成依赖安装、类型检查和 `npm run build` 生产构建。
2. 执行 Compose config、构建启动、ps、健康/就绪检查，确认数据库/后端 healthy 和前端可访问。
3. 至少 8 项接口冒烟：登录、储罐创建、期初/期末快照创建、流入/流出创建、平衡运行、缺失快照拒绝、不确定度查询、提交复核、接受/拒绝和审计查询；真实结果只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只用 Codex 内置 Browser 验证登录、5 页面、质量瀑布图、证据明细、运行与复核交互；禁止外部 Chrome，截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 执行 `docker compose down -v --remove-orphans` 并确认本项目无容器残留，不影响其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
